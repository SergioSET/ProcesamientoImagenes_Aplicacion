Para ejecutar este código debes tener instalado Python.

Además debes instalar las dependencias con el siguiente comando:
pip install -r requirements.txt

Para ejecutar el código simplemente ejecuta:
python main.py